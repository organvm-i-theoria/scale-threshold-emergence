Rebuild the entire UAKS repository locally from the supplied conversation artifacts by externalizing every drafted specification into its own file, preserving all IDs, ontology boundaries, trust states, relation types, manifests, and lifecycle rules, creating placeholders for any still-undrafted documents, generating the full directory structure, bootstrap scripts, seed data folders, and implementation scaffolding.
